# how to install eviroment

``` python -m venv venv ```

##### Linux/MacOS:
``` source venv/bin/activate ```

#### Windows:
``` venv\Scripts\activate ```


#### Then execute:
``` pip install -r requirements.txt ```
# FlaskList
